<header id="journal-header"{{#if @blog.cover}} style="background-image: url('{{@blog.cover}}');"{{/if}}>
  <span>
    <div class="container">
      <div class="col-3 col-offset-1">
        <h1>{{@blog.title}}</h1>
        <h3>{{@blog.description}}</h3>
      </div>
    </div>
  </span>
</header>

{{> navigation}}
